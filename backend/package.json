{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:backup": "dotenv -e .env -- pg_dump \"%DATABASE_URL%\" -F c -b -v -f ./backup/backup.dump",
    "db:restore": "dotenv -e .env -- pg_restore --clean --no-owner -d \"%DATABASE_URL%\" ./backup/backup.dump"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/adapter-pg": "^7.10.0",
    "@prisma/client": "^7.10.0",
    "bcrypt": "^5.1.1",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/node": "^26.6.2",
    "dotenv-cli": "^11.0.0",
    "prisma": "^6.19.3"
  },
  "prisma": {
    "seed": "node prisma/seeds/seed.js"
  }
}
